import React from 'react';

const InterviewPage = () => {
  // Placeholder for the interview page content
  // You can retrieve interviewData passed via navigate state if needed
  // const location = useLocation();
  // const { interviewData } = location.state || {};

  return (
    <div>
      <h1>Interview Page</h1>
      <p>Interview will start here...</p>
      {/* Display interviewData if needed */}
    </div>
  );
};

export default InterviewPage; 